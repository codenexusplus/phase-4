apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "todo-chatbot.fullname" . }}-db-config
  labels:
    {{- include "todo-chatbot.labels" . | nindent 4 }}
data:
  DATABASE_URL: "postgresql://postgres:postgrespass@{{ include "todo-chatbot.fullname" . }}-postgres:5432/todo_app"